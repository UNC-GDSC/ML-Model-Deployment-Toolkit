apiVersion: v1
kind: Secret
metadata:
  name: ml-model-secrets
type: Opaque
stringData:
  API_KEY: "change-this-to-your-secret-api-key"
  JWT_SECRET: "change-this-to-your-jwt-secret"
  # Add base64 encoded secrets
  # API_KEY: <base64-encoded-key>
  # JWT_SECRET: <base64-encoded-secret>
