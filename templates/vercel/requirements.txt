# Lightweight dependencies for Vercel
# Keep this minimal due to size constraints

numpy==1.24.3
scikit-learn==1.3.0
joblib==1.3.2
pydantic==2.4.2

# Note: TensorFlow and PyTorch are too large for Vercel
# Use lightweight models only (sklearn, ONNX with onnxruntime-slim)
